# Environment variable templates are split per app — see:
#   apps/api/.env.example   ← FastAPI backend (secrets, DB, Redis, API keys)
#   apps/web/.env.example   ← Next.js frontend (NEXT_PUBLIC_ vars only)

